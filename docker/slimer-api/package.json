{
  "name": "slimer-api",
  "version": "1.0.0",
  "private": true,
  "description": "Tor hidden service management API for slimer VM",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "mocha test/**/*.test.js --timeout 10000"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "mocha": "^10.7.0"
  }
}
